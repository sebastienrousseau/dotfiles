# Demo Capture

Goal: a 30s walkthrough GIF for the README.

Suggested steps:
1) `dot theme set tokyonight-night`
2) Open a terminal and run: `dot keys`
3) Open Neovim and show a quick search (Telescope)
4) Show `dot theme toggle`
5) Show `dot wallpaper`

Tools:
- macOS: QuickTime or `ffmpeg`
- Linux: `wf-recorder` or `ffmpeg`

Output:
- Save as `docs/demo/demo.gif`
