# GPG Agent Defaults
# Cache TTLs in seconds (1 day = 86400 seconds)

default-cache-ttl 86400
max-cache-ttl 86400

# Enable SSH agent support (optional)
# enable-ssh-support

# Use pinentry if available
# pinentry-program /usr/bin/pinentry-gnome3
