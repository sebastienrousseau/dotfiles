# GPG Agent Defaults
# Cache TTLs in seconds (7 days = 604800 seconds)

default-cache-ttl 604800
max-cache-ttl 604800

# Enable SSH agent support (optional)
# enable-ssh-support

# Use pinentry if available
# pinentry-program /usr/bin/pinentry-gnome3
