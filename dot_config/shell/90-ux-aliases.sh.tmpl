#!/usr/bin/env bash

# Universal Aliases (Generated by Chezmoi)
if [ -n "${BASH_VERSION:-}" ]; then
  shopt -s expand_aliases
fi

# Common Aliases (Git, NPM, General)
{{- $globPattern := joinPath .chezmoi.sourceDir ".chezmoitemplates/aliases/**/*.aliases.sh" }}
{{- $files := glob $globPattern }}
{{- range $files }}
  {{- if not (contains "/macOS/" .) }}
    {{- include . }}
    {{- "\n" }}
  {{- end }}
{{- end }}

# OS-Specific Aliases

## macOS
{{- if eq .chezmoi.os "darwin" }}
  {{- $macGlobPattern := joinPath .chezmoi.sourceDir ".chezmoitemplates/aliases/macOS/*.aliases.sh" }}
  {{- $macFiles := glob $macGlobPattern }}
  {{- range $macFiles }}
    {{- include . }}
    {{- "\n" }}
  {{- end }}
{{- end }}

# Note: Linux/Windows specific aliases can be added here in similar blocks
