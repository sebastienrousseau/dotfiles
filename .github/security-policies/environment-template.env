# Environment Variables Template for Secure Configuration
# Copy this to .env.local and configure with actual values
# NEVER commit .env.local to version control

# ============================================================================
# REQUIRED ENVIRONMENT VARIABLES
# ============================================================================

# GitHub Configuration
# Used by: install/lib/installers.sh, GitHub Actions
# GITHUB_TOKEN="<your-github-token>"
GITHUB_TOKEN="${GITHUB_TOKEN:-}"

# GPG Configuration
# Used by: git configuration, commit signing
# GPG_SIGNING_KEY="<your-key-fingerprint>" # gitleaks:allow
GPG_SIGNING_KEY="${GPG_SIGNING_KEY:-}" # gitleaks:allow

# ============================================================================
# OPTIONAL ENVIRONMENT VARIABLES
# ============================================================================

# Chezmoi Configuration
# CHEZMOI_SOURCE_DIR="${HOME}/.dotfiles"
CHEZMOI_SOURCE_DIR="${CHEZMOI_SOURCE_DIR:-${HOME}/.dotfiles}"

# Development Environment
# NODE_ENV="development"
NODE_ENV="${NODE_ENV:-development}"

# Terminal Configuration
# TERM="xterm-256color"
TERM="${TERM:-xterm-256color}"

# Shell Configuration
# SHELL="/bin/zsh"
SHELL="${SHELL:-/bin/bash}"

# ============================================================================
# SECURITY-SPECIFIC VARIABLES
# ============================================================================

# Security scanning configuration
# SECURITY_SCAN_ENABLED="true"
SECURITY_SCAN_ENABLED="${SECURITY_SCAN_ENABLED:-true}"

# Vulnerability database update frequency (hours)
# VULN_DB_UPDATE_FREQUENCY="24"
VULN_DB_UPDATE_FREQUENCY="${VULN_DB_UPDATE_FREQUENCY:-24}"

# Security policy enforcement level (strict, moderate, permissive)
# SECURITY_POLICY_LEVEL="strict"
SECURITY_POLICY_LEVEL="${SECURITY_POLICY_LEVEL:-moderate}"

# ============================================================================
# CI/CD SPECIFIC VARIABLES (GitHub Actions)
# ============================================================================

# Build timeout in minutes
# BUILD_TIMEOUT="30"
BUILD_TIMEOUT="${BUILD_TIMEOUT:-30}"

# Test timeout in minutes
# TEST_TIMEOUT="15"
TEST_TIMEOUT="${TEST_TIMEOUT:-15}"

# Security scan timeout in minutes
# SECURITY_SCAN_TIMEOUT="20"
SECURITY_SCAN_TIMEOUT="${SECURITY_SCAN_TIMEOUT:-20}"

# ============================================================================
# CLOUD PROVIDER CREDENTIALS (if needed)
# ============================================================================

# AWS Configuration (uncomment if needed)
# AWS_ACCESS_KEY_ID="${AWS_ACCESS_KEY_ID:-}"
# AWS_SECRET_ACCESS_KEY="${AWS_SECRET_ACCESS_KEY:-}"
# AWS_DEFAULT_REGION="${AWS_DEFAULT_REGION:-us-west-2}"

# GCP Configuration (uncomment if needed)
# GOOGLE_APPLICATION_CREDENTIALS="${GOOGLE_APPLICATION_CREDENTIALS:-}"
# GOOGLE_CLOUD_PROJECT="${GOOGLE_CLOUD_PROJECT:-}"

# Azure Configuration (uncomment if needed)
# AZURE_CLIENT_ID="${AZURE_CLIENT_ID:-}"
# AZURE_CLIENT_SECRET="${AZURE_CLIENT_SECRET:-}"
# AZURE_TENANT_ID="${AZURE_TENANT_ID:-}"

# ============================================================================
# USAGE INSTRUCTIONS
# ============================================================================

# 1. Copy this file to .env.local:
#    cp .github/security-policies/environment-template.env .env.local

# 2. Configure required variables by uncommenting and setting values:
#    GITHUB_TOKEN="your_actual_token_here"

# 3. Source the environment file before running scripts:
#    source .env.local

# 4. For GitHub Actions, set secrets in repository settings:
#    - Go to Settings → Secrets and variables → Actions
#    - Add each required variable as a secret

# ============================================================================
# SECURITY NOTES
# ============================================================================

# - Never commit actual credentials to version control
# - Use different tokens for different environments (dev, staging, prod)
# - Rotate tokens regularly (every 90 days recommended)
# - Use least-privilege principle when creating tokens
# - Monitor token usage in audit logs
# - Consider using credential managers (1Password, HashiCorp Vault, etc.)