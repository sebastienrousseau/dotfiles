#!/usr/bin/env bash
# Copyright (c) 2015-2026 Dotfiles. All rights reserved.
## mkscript — Create a ready-to-go shell script.
##
## # Usage
##   mkscript hello
##   mkscript /tmp/myscript

set -euo pipefail

file="${1:-}"

if [[ -z "${file}" ]]; then
  echo "Usage: mkscript <path>" >&2
  exit 1
fi

if [[ -f "${file}" ]]; then
  echo "${file} already exists, aborting." >&2
  exit 1
fi

mkdir -p "$(dirname "${file}")"

cat >"${file}" <<'EOF'
#!/usr/bin/env bash
# Copyright (c) 2015-2026 Dotfiles. All rights reserved.

set -euo pipefail

echo "hello world"
EOF

chmod +x "${file}"
echo "Created ${file}"
