# dot bash completion — generated by: dot completion bash
# Source this file, or install it to a bash-completion completions dir.
_dot_completions() {
  local cur prev commands
  COMPREPLY=()
  cur="${COMP_WORDS[COMP_CWORD]}"
  prev="${COMP_WORDS[COMP_CWORD - 1]}"
  commands='sync add diff status edit uninstall cd commit doctor attest secret-audit score snapshot chaos bundle rollback history perf benchmark metrics load-bench ai ai-setup ai-query mcp mode agent cl copilot kimi agy kiro sgpt ollama opencode aider env profile keys tools new sandbox lint learn docs theme wallpaper fonts tune secrets-init secrets secrets-create ssh-key ssh-cert backup policy encrypt-check firewall telemetry dns-doh lock-screen usb-safety fleet teleport upgrade packages cache-refresh completion search version help'
  case "$prev" in
    dot)
      COMPREPLY=($(compgen -W "$commands" -- "$cur"))
      return 0
      ;;
    agent)
      COMPREPLY=($(compgen -W "card log checkpoint delegate a2a-card conformance" -- "$cur"))
      return 0
      ;;
    agents)
      COMPREPLY=($(compgen -W "list check render" -- "$cur"))
      return 0
      ;;
    ai)
      COMPREPLY=($(compgen -W "chat tools install serve cost login doctor ask run delegate" -- "$cur"))
      return 0
      ;;
    aliases)
      COMPREPLY=($(compgen -W "list search why stats cheatsheet tiers" -- "$cur"))
      return 0
      ;;
    env)
      COMPREPLY=($(compgen -W "list emit prune install use" -- "$cur"))
      return 0
      ;;
    fleet)
      COMPREPLY=($(compgen -W "drift namespace events status enforce apply" -- "$cur"))
      return 0
      ;;
    keys)
      COMPREPLY=($(compgen -W "sign-check" -- "$cur"))
      return 0
      ;;
    mcp)
      COMPREPLY=($(compgen -W "doctor registry serve" -- "$cur"))
      return 0
      ;;
    mode)
      COMPREPLY=($(compgen -W "list current show set run doctor" -- "$cur"))
      return 0
      ;;
    patterns)
      COMPREPLY=($(compgen -W "list view edit" -- "$cur"))
      return 0
      ;;
    profile)
      COMPREPLY=($(compgen -W "show set" -- "$cur"))
      return 0
      ;;
    registry)
      COMPREPLY=($(compgen -W "list search info install url set-url" -- "$cur"))
      return 0
      ;;
    secrets)
      COMPREPLY=($(compgen -W "edit set get list load provider" -- "$cur"))
      return 0
      ;;
    theme)
      COMPREPLY=($(compgen -W "list set toggle sync family current rebuild" -- "$cur"))
      return 0
      ;;
    tools)
      COMPREPLY=($(compgen -W "install docs" -- "$cur"))
      return 0
      ;;
    wallpaper)
      COMPREPLY=($(compgen -W "sync rotate" -- "$cur"))
      return 0
      ;;
  esac
  COMPREPLY=($(compgen -f -- "$cur"))
}
complete -F _dot_completions dot
