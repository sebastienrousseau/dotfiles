# GPG Agent Defaults
# Cache TTLs in seconds (1 hour = 3600 seconds)

default-cache-ttl 3600
max-cache-ttl 3600

# Enable SSH agent support (optional)
# enable-ssh-support

# Use pinentry if available
# pinentry-program /usr/bin/pinentry-gnome3
