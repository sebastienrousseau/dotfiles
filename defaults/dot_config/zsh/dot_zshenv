# PATH is assembled centrally in dot_config/shell/00-core-paths.sh.tmpl (sourced by .zshrc).
# Minimal early bootstrap (~/.local/bin) is in ~/.zshenv.

# Skip /etc/zprofile (path_helper ~3ms) and /etc/zshrc (terminfo ~10ms) — we handle both
setopt NO_GLOBAL_RCS
