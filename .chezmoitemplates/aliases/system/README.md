# System Aliases

Consolidated runtime/system helpers:

- process views (`ps`, `psa`, `pid`)
- network quick checks (`nls`, `op`, `ports`)
- host/runtime helpers (`p`, `path`, `h`, `wth`, `wip`)
- platform-safe refresh helper (`update-fonts`)
- guarded nmap shortcuts (`nma*`, `nm*`)
