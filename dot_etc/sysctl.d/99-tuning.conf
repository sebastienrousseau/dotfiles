# Increase file descriptor limits (useful for heavy workloads/servers, good for dev machines too)
fs.file-max = 2097152

# TCP Optimization
# Use BBR for congestion control (requires tcp_bbr module loaded)
net.core.default_qdisc = fq
net.ipv4.tcp_congestion_control = bbr
