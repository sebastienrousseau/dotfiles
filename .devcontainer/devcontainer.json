{
  "name": "dotfiles",
  "image": "mcr.microsoft.com/devcontainers/base:ubuntu",
  "postCreateCommand": "sudo apt-get update && sudo apt-get install -y git curl zsh && sh -c \"$(curl -fsLS get.chezmoi.io)\" -- -b $HOME/.local/bin && $HOME/.local/bin/chezmoi apply --source $PWD --exclude scripts",
  "customizations": {
    "vscode": {
      "settings": {
        "terminal.integrated.defaultProfile.linux": "zsh"
      }
    }
  }
}
